{
  "name": "backend-settlement",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend/src/modules/settlement",
  "projectType": "library",
  "tags": ["type:lib", "scope:backend", "domain:settlement", "layer:domain"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["apps/backend/src/modules/settlement/**/*.ts"]
      }
    }
  }
}
