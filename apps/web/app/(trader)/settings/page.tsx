/**
 * @file page.tsx
 * @module web
 * @description Settings route showing security, sessions, API keys, and preferences.
 * @author BharatERP
 * @created 2026-04-16
 */

import { SettingsPage as SettingsFeature } from '@/features/settings';

export default function SettingsPage() {
  return <SettingsFeature />;
}
